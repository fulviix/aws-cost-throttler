services:
  redis:
    image: redis:7-alpine
    container_name: aws-throttler-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --save 60 1 --loglevel warning

volumes:
  redis-data: